//Formas de declarar variables
var miVariable; // debe cambiar a medida que se utiliza -- es una manera generica por lo que se recomienda usar las otras dos formas
let nombre; // cuando la variable cambia
const IVA = 0.21; //debe inicializarse dado que no cambia